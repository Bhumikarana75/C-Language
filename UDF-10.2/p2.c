// WAP to find the length of the String by passing a string as an argument using UDF.

void main(){	
	
	string();
	
}

int string(){
	char str[10];
	
	printf("Enter name: ");
	gets(str);
	
	int length = strlen(str);
	
	printf("%s ",str);
	printf("\nlength of string is %d ",length);
}
